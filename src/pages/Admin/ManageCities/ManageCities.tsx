import { Typography } from "@mui/material"

export default function ManageCities() {
    return (
        <div>
            <Typography variant="h4">
                Manage Cities
            </Typography>
        </div>
    )
}